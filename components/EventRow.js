import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const EventRow = ({eventDetails}) => {
  return (
    <View>
      <Text>EventRow</Text>
    </View>
  )
}

export default EventRow

const styles = StyleSheet.create({})